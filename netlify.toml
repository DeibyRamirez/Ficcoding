[build]
  command = "./build.sh"
  publish = "build/web"

[build.environment]
  PATH = "/opt/buildhome/flutter/bin:/opt/buildhome/flutter/bin/cache/dart-sdk/bin:/usr/local/bin:/usr/bin:/bin"

[context.production.environment]
  GO_VERSION = "1.18.x"  # Asegúrate de colocar la misma versión que especificaste en build.sh
